// neural-nexus-frontend/next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
    // Add the output option here
    output: 'standalone',
  
    // Other existing configurations (if any)
    // For example:
    // reactStrictMode: true,
    // images: { ... },
  };
  
  export default nextConfig;
  